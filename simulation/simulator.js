// const { addPatient } = require('../services/queueService');

// const names = ['Alice', 'Bob', 'Charlie', 'David'];
// function randomPatient() {
//   const name = names[Math.floor(Math.random() * names.length)];
//   const triageLevel = Math.floor(Math.random() * 5) + 1;
//   return addPatient(name, triageLevel);
// }

// setInterval(() => {
//   const patient = randomPatient();
//   console.log(`Simulated patient: ${patient.name} (Level ${patient.triageLevel})`);
// }, 5000);
